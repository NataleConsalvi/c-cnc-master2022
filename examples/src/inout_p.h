#include <stdio.h>
#include <stdlib.h>

//To print the content of our array (given the array and its size)
void array_print(float *a, int n);

float* f1 (int n);

void f2(float *a, int n);

void f3(float **a, int n);